<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.my.project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bases.BaseActivity" />
        <activity android:name=".study.asynctask.AsyncTaskActivity" />
        <activity android:name=".study.recyclerview.RecyclerViewActivity" />
        <activity android:name=".study.recyclerview.demo.HomeActivity" />
        <activity android:name=".study.recyclerview.demo.StaggeredGridLayoutActivity" />
        <activity
            android:name=".study.recyclerview.photo.RecyclerViewPhotoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".study.recyclerview.photo.PhotoDetalsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".study.recyclerview.photo.DetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".study.okhttp.OkHttpDemoActivity" />
        <activity android:name=".update.activity.UpdateActivity" />
        <activity android:name=".study.EncryptActivity"></activity>
    </application>

</manifest>
